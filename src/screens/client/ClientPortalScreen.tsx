import React from 'react';
import { View, Text, ScrollView } from 'react-native';

// Client portal with vehicle health and service history
export default function ClientPortalScreen() {
  return (
    <ScrollView>
      <Text>Client Portal</Text>
    </ScrollView>
  );
}